{
  "name": "@guardiandb/postgres-typeorm",
  "version": "0.1.0",
  "description": "GuardianDB-aware TypeORM DataSource (manages an embedded PostgreSQL-compatible gateway)",
  "license": "MIT OR Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "peerDependencies": {
    "typeorm": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "pg": "^8.11.0",
    "reflect-metadata": "^0.2.2",
    "tsx": "^4.7.0",
    "typeorm": "^0.3.20",
    "typescript": "^5.4.0"
  }
}
